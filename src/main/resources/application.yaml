spring:
  application:
    name: "JavaShareHub"
  mvc:
    static-path-pattern: /static/**
  freemarker:
    expose-session-attributes: true

  datasource:
    url: jdbc:h2:./db/JavaShareHubDB;AUTO_SERVER=TRUE
    username: root
    password: qwe
    driver-class-name: org.h2.Driver

  liquibase:
    change-log: classpath:/db/changelog/master.yml
  jpa:
    hibernate:
      ddl-auto: validate


logging:
  level:
    root: info
    org:
      springframework:
        security: debug
  file:
    path: logs
